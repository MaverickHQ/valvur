# valvur's own scan configuration — committed deliberately, reviewed on expiry.

# The dogfooding gate scans this repository. tests/fixtures holds deliberately
# vulnerable code: planted secrets, known-bad dependency manifests, prompt-injection
# payloads and hidden Unicode. It is the input our tests assert against, so it must
# stay exactly as broken as it is.
#
# Unexcluded it produced 61 of 64 self-scan findings, which makes the gate useless:
# a signal that is 95% intentional is not a signal. The scan still runs over it —
# this drops the findings and reports the count, so SUMMARY.md and run.json both say
# what was excluded and what it cost.
#
# This is a per-project decision in a committed file, never a built-in default.
# Skipping every project's tests would hide real code from the people who most need
# to see it.
[scan]
exclude = ["tests/fixtures"]

# valvur suppressions — committed deliberately, reviewed on expiry.
#
# Every entry needs a fingerprint, rule, path, expiry and reason. The expiry is
# mandatory by design: an accepted risk that never comes back up for review is an
# accepted risk nobody is accepting any more.
#
# These two are Checkov rules that do not apply to a one-shot scanner image. Neither
# is a defect to fix later — they are the rule being wrong about this container — so
# they carry a long expiry and a reason that says what would change the answer.

[[suppress]]
fingerprint = "6addff03638bb8538647b6b3ea9e90df"
rule = "CKV_DOCKER_4"
path = "Dockerfile"
expires = 2027-08-31
reason = "ADD is required here: lines 31-32 fetch the Opengrep release binary over HTTPS, pinned to v1.29.0 with --chmod. COPY cannot fetch a URL, so the rule's remedy does not apply. No archive is auto-extracted, which is the behaviour CKV_DOCKER_4 actually guards against. Revisit if the download moves into a builder stage."

[[suppress]]
fingerprint = "0e2cb52a1e48b5c3e8507724afdc67b4"
rule = "CKV_DOCKER_2"
path = "Dockerfile"
expires = 2027-08-31
reason = "No HEALTHCHECK: this image runs one scanner command and exits. A healthcheck describes a long-running service, and Docker never runs one for a container that has already finished. Adding it would be cargo cult. Revisit if valvur ever ships a daemon or server mode."

[[suppress]]
fingerprint = "f8d0842b5a5b319120a3f06e6f77b23a"
rule = "valvur.licence.dependency-unknown"
path = "sbom.cdx.json"
expires = 2027-09-01
reason = "Not a compliance gap: all four DO declare licences, through PyPI classifiers that Syft does not read. Verified 2026-09-01 — docutils (BSD/GPL/public domain), id (Apache-2.0), markdown-it-py (MIT), pathspec (MPL-2.0). Syft records licences for 36 of 140 components here, so the blind spot is detection, not declaration. valvur cannot fix this generally: it has only the SBOM, and the scanned project's packages are not installed on the host to read classifiers from. Revisit when Syft reads Trove classifiers, or if a genuinely unlicensed dependency appears in this list."

[[suppress]]
fingerprint = "b931bd3d8aedd259c03494ed10463d78"
rule = "CKV_DOCKER_7"
path = "Dockerfile"
expires = 2027-09-05
reason = "Not a registry image: 'opengrep-${TARGETARCH}' names an internal build stage defined above, selected by the platform being built (task 15.4). There is no tag to pin, and the stages it resolves to are themselves pinned by digest. Checkov's dockerfile parser does not resolve inter-stage references through a build argument, so it reads the absent tag as 'latest'. Revisit if Checkov gains stage-aware parsing, or if this FROM ever names a real image."
